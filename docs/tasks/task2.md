## 2. Phase 1 後半: bible ID・URL・Markdown変換・エディタ連携

@docs/todo.md の以下を実装して。

- 1.6 bible ID生成
- 1.7 URL生成
- 1.8 Markdownリンク生成
- 1.9 エディタ連携
- 1.10 デバウンス
- 1.11 有効・無効制御

要件:
- Phase 1 前半で作ったパーサーを使う
- 現在フォーカス中の行だけを対象にする
- 入力停止から設定値 `debounceMs` 後に変換する
- 初期値は1000ms
- `enabled: false` の場合は変換しない
- 変換後の表示テキストは元入力を保持する

完了条件:
- Obsidian上で `テトス1:14` が自動的にMarkdownリンクへ変換される
- `テトス１：１４` も正しい bible ID で変換される
- 不完全入力中は変換されない
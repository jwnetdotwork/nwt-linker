TARGET_DIR="/Users/ship/Library/Mobile Documents/iCloud~md~obsidian/Documents/Default/.obsidian/plugins/nwt-linker"

# Create the target directory if it doesn't exist
mkdir -p "$TARGET_DIR"

# Copy the necessary files to the target directory
cp main.js "$TARGET_DIR"
cp manifest.json "$TARGET_DIR"